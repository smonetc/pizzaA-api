CREATE TABLE yourpizza(
   id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
   crust TEXT NOT NULL,
   cheese TEXT NOT NULL,
   sauce TEXT NOT NULL,
   meats TEXT NOT NULL,
   toppings TEXT NOT NULL,
   category_id INTEGER
      REFERENCES category(id) ON DELETE CASCADE NOT NULL
);